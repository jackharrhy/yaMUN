CREATE TABLE "public"."section" ("id" serial NOT NULL, "sid" text NOT NULL, "section" text NOT NULL, "crn" integer NOT NULL, "schedule_type" text, "phone_one" text NOT NULL, "phone_two" text NOT NULL, "wait_list" boolean NOT NULL, "pre_check" boolean NOT NULL, "reserved" text, "attr" text NOT NULL, "credit_hours" integer, "billed_hours" integer, "primary_instructor" integer NOT NULL, "secondary_instructor" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("primary_instructor") REFERENCES "public"."people"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("secondary_instructor") REFERENCES "public"."people"("id") ON UPDATE cascade ON DELETE cascade);
