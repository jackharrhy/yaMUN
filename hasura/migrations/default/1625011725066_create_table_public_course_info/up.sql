CREATE TABLE "public"."course_info" ("id" integer NOT NULL, "subject" text NOT NULL, "number" text NOT NULL, "title" text NOT NULL, "description" Text NOT NULL, "attributes" text[] NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("subject") REFERENCES "public"."subject"("name") ON UPDATE cascade ON DELETE restrict, FOREIGN KEY ("number") REFERENCES "public"."course_number"("name") ON UPDATE cascade ON DELETE restrict);
