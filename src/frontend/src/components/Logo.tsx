import React from "react";

function Logo() {
  return (
    <div
      style={{
        width: "7em",
        padding: "0.25em",
        fontSize: "1.5em",
      }}
      className="text-center hover:text-gray-200 rounded-md shadow-sm bg-gradient-to-r from-red-400 via-red-500 to-red-600 text-white font-extrabold"
    >
      <p>yaMUN</p>
    </div>
  );
}

export default Logo;
